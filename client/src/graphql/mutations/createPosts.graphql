mutation CreatePosts(
  $title: String!
  $content: String!
  $imageUrl: String! | null
  $spaceName: String!
) {
  createPosts(
    title: $title
    content: $content
    imageUrl: $imageUrl
    spaceName: $spaceName
  ) {
    ...PostSnippet
  }
}
